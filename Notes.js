/**
 * 
 * ! Introducing React Fragment
 *  * It's an inbuit wrapper 
 *  * It's an empty wrapper component. 
 *  * It doesn't add any real HTML element to the DOM, but it fulfills React's JSX's requirement.
 * 
 * ! Introduction to ref and useRef
 * * Ref allow us to access other DOM elements and to work with them.
 */